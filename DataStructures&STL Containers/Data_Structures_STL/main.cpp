#include <bits/stdc++.h>

using namespace std;

int main() {
     // container is an object that stores a collection of other subjects;
     // container manages the storages space for its elements & provides member functions to access them ( iterator & object & variable )

     // Sequence containers ( linearly )
     //  Array
     // Vector
     // Deque
     // Forward list
     // List

     // Associative container (ordered,tree)
     // set (sort by value)
     // map (sort by key)
     // multiset
     // multimap

     // Unordered Associative container (Random)
     // Unordered set (searching at O(1) )
     // Unordered map (HashTable) (insert,search,delete at O(1))
     // Unordered multiset
     // Unordered multimap

     // container adaptors
     // Stack (Lookup top element, remove at top element)
     // Queue (push element at back, remove at front)
     // Priority Queue (Heap) (Get,remove max,min element, insert elements) (O(log(n))
    return 0;
}
