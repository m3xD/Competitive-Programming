#include <bits/stdc++.h>

using namespace std;

int main() {
     // linearly of the expectation
     // E[x1+x2] = E[x1] + E[x2]
     // example of e[z] = {e1,e2,....,en} (coin)
     // e[ei] = sigma (xi * pi ) = 0.5 (head = 1, tail = 0)
     // ==> E(z) = 0.5 * n;
     // Bernoulli Trial
     // 1) if the probability of success of bernoulli is p then the expected number of trials is 1/p
     // 2) if the probability of getting success of bernoulli is p then the expected number of success in n trials is np
    return 0;
}
